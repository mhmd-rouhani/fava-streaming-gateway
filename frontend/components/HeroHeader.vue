<template>
  <header class="mb-8">
    <p
      class="mb-[0.35rem] bg-gradient-to-bl from-white from-20% via-accent-2 via-55% to-accent bg-clip-text text-[clamp(2.4rem,6vw,3.4rem)] font-bold tracking-tight text-transparent"
    >
      فاوا
    </p>
    <h1
      class="mb-[0.6rem] text-[clamp(1.15rem,2.4vw,1.45rem)] font-medium tracking-tight text-ink"
    >
      درگاه استریم فایل
    </h1>
    <p class="m-0 max-w-[42rem] text-[0.98rem] leading-relaxed text-muted">
      فایل‌های حجیم را بدون ذخیره روی دیسک و بدون نگه داشتن کل فایل در حافظه آپلود کنید.
      فایل‌ها از طریق Express به‌صورت استریم مستقیم وارد MinIO (S3) می‌شوند.
    </p>
  </header>
</template>
